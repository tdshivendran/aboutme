import React, { Component } from 'react';
import MainPage from './MainPage';

const display = [
    <div>
        <div>
            <MainPage/>
        </div>
    </div>

];

class App extends Component {
    render() {
        return (display);
    }
}

export default App;
